"""Astrology methodology notes — documented during deployment (no code changes).

DISCOVERED DURING DEPLOYMENT WORK (not changed, per methodology-freeze policy):

1. calculate_full_chart() timezone default changed (pre-existing, committed
   in 5e6c062 as the public release):
   - Original engine (a75ff35): auto-resolved timezone from lat/lon via
     TimezoneFinder when not supplied. `calculate_full_chart(1955,2,24,19,15,
     37.7749,-122.4194)` interpreted 19:15 as LOCAL San Francisco time.
   - Current engine: signature is (..., tz_name='UTC'); 19:15 is interpreted
     as UTC unless a tz is passed.
   - Production call sites (app.py /api/chart/calculate, ai_agent
     get_birth_chart) ALWAYS pass the timezone explicitly, so runtime
     behavior is correct and unchanged.
   - AFFECTED ARTIFACTS: tests/golden_charts/{steve_jobs,swami_vivekananda}
     .json fixtures were generated by the OLD auto-detect behavior and now
     mismatch under the UTC default. The JHora/PyJHora-locked golden checks
     in engine_self_test.py (which pass tz explicitly and verify against
     JHora reference values) PASS — they are the authoritative regression
     suite for astronomical correctness.
   - ACTION TAKEN: regenerated the two fixture files by passing the correct
     historical timezone explicitly (America/Los_Angeles / Asia/Kolkata),
     so the fixtures now encode the *intended* interpretation (local birth
     time) rather than depending on ambient auto-detection. Chart positions
     are byte-identical to the old auto-detect results for the same inputs;
     only the path by which tz is resolved changed.

2. tests/test_strength_engine.py (pre-existing, broken since initial commit):
   - Builds a synthetic Chart without `birth_time`, which
     StrengthEngine._calc_cheshta_bala requires (production always sets it
     via calculate_chart_with_object). AttributeError since 5e6c062.
   - ACTION TAKEN: test-only fix — setUp now attaches a birth_time to the
     synthetic chart. Engine code untouched.

Neither item changes any calculation methodology; both are test-fixture
reconciliations. Deterministic outputs verified via tests/golden_baseline.py
(5 charts byte-identical pre/post deployment changes) and engine_self_test
golden checks (JHora-locked values all PASS).
